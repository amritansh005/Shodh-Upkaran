fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27

# Azure OpenAI — used by llm_client.py (QA answers)
openai>=1.0.0

# PyMuPDF — PDF page count detection before passing to Marker
pymupdf>=1.24.0

# PostgreSQL + pgvector
psycopg2-binary>=2.9
pgvector>=0.2.5

# Embeddings (local, no API key)
sentence-transformers>=3.0.0  # BAAI/bge-large-en-v1.5

# PDF extraction — Marker (layout detection + OCR on GPU)
# IMPORTANT: Install torch FIRST with the special index URL, then run pip install -r requirements.txt.
# marker-pdf may downgrade torch — reinstall torch after if needed.
marker-pdf>=1.10.0

# transformers — pinned to 4.56.2 for surya-ocr 0.17.x compatibility.
# surya 0.17.1 was written against transformers ~4.56.x.
# transformers 5.x removed APIs (find_pruneable_heads_and_indices, ROPE_INIT_FUNCTIONS shape, etc.)
# that surya depends on — do NOT upgrade beyond 4.56.x.
transformers==4.56.2

# Pillow — surya requires <11, torchvision may pull 12+ so we pin it.
Pillow>=10.2.0,<11.0.0

# PyTorch with CUDA 11.8 — install in this order:
#
#   Step 1 — Install torch FIRST (needs special index URL, cannot go in requirements.txt):
#     pip install "torch==2.5.1+cu118" "torchvision==0.20.1+cu118" --index-url https://download.pytorch.org/whl/cu118
#
#   Step 2 — Install everything else:
#     pip install -r requirements.txt
#
#   Step 3 — Reinstall torch (marker-pdf or transformers may have downgraded it):
#     pip install "torch==2.5.1+cu118" "torchvision==0.20.1+cu118" --index-url https://download.pytorch.org/whl/cu118 --force-reinstall
#
# Pinned versions:
# torch==2.5.1+cu118
# torchvision==0.20.1+cu118