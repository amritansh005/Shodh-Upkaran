fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
openai>=1.0.0

# Docling (layout-aware PDF conversion; open source)
docling>=2.0.0

# PyMuPDF — used only for page counting and byte-level slicing (CPU, no text extraction)
pymupdf>=1.24.0

# PostgreSQL + pgvector
psycopg2-binary>=2.9
pgvector>=0.2.5

# Embeddings (local, no API key)
sentence-transformers>=3.0.0  # BAAI/bge-large-en-v1.5

# PyTorch with CUDA 12.1 support (for GPU-accelerated Docling)
# Install manually with:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0